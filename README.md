# Potato Leaf Disease Classification 🌱

This is a **college project** from the CSE department to detect diseases in potato leaves using machine learning.

## 📌 Project Overview
- Detects common potato leaf diseases from images.
- Uses deep learning (TensorFlow/Keras) for classification.
- Helps farmers identify plant health issues early.

## ⚙️ Tech Stack
- Python
- TensorFlow / Keras
- Flask (for web/WhatsApp bot deployment)
- Twilio API (WhatsApp integration)
- Railway (cloud deployment)

## 🚀 How to Run Locally
1. Clone the repository:
   ```bash
   git clone https://github.com/Suraj123781/potato-leaf-disease-classification.git
   cd potato-leaf-disease-classification
2.- Create a virtual environment:
python -m venv venv
source venv/bin/activate   # Linux/Mac
venv\Scripts\activate      # Windows

3.- Install dependencies:
 pip install -r requirements.txt

4. Run the app:
   - python app.py
